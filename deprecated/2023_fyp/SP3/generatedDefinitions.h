#pragma once
#include <iostream>

using namespace std;

namespace generatedDefinitions {
	typedef enum State {
		STATE1,
		STATE2,
		STATE3
	} State_t;

	typedef enum Event {
		NONE,
		EVENT1,
		EVENT2,
		EVENT3
	} Event_t;
}

